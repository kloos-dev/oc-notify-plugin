# ===================================
#  List Column Definitions
# ===================================

columns:
    created_at:
        label: kloos.notify::lang.field.created_at
        type: datetime

    published_at:
        label: kloos.notify::lang.field.published_at
        type: datetime

    title:
        label: kloos.notify::lang.field.title
        type: text